import { Reporter, AggregatedResult, Test, TestResult, TestContext } from '@jest/reporters';

declare class DiscoveryReporter implements Reporter {
    private specsWitoutResultsCount;
    private fullTestSuite;
    onRunStart(results: AggregatedResult): void;
    onTestFileResult(test: Test, testResult: TestResult): Promise<void> | void;
    onRunComplete(testContexts: Set<TestContext>, aggregatedResults: AggregatedResult): Promise<void>;
    private getFullTestSuite;
}

export { DiscoveryReporter as default };
